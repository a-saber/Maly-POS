import 'package:flutter/material.dart';

class AppBordersSizes {
  static const double fields = 5;
}

class AppBordersRaduis {
  static const BorderRadius btn =
      BorderRadius.all(Radius.circular(AppBordersSizes.fields));
  static const BorderRadius form =
      BorderRadius.all(Radius.circular(AppBordersSizes.fields));
}
